README FOR geckoboard_appsignal
===============================

This is a small Sinatra app which reads the (rather bad) AppSignal API, aggregates the data it recieves
and provides a JSON hash for Geckoboard to query.

